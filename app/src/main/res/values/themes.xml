<resources>
    <!-- Base application theme. -->
    <style name="Theme.SistemaETR" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/etr_black</item>
        <item name="colorOnPrimary">@color/etr_white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/etr_red</item>
        <item name="colorOnSecondary">@color/etr_white</item>
        <!-- Accent color. -->
        <item name="android:colorBackground">@color/etr_background_light</item>
        <item name="colorSurface">@color/etr_surface_light</item>
        <!-- Background and surface colors. -->
        <item name="colorOnBackground">@color/etr_text_primary_light</item>
        <item name="colorOnSurface">@color/etr_text_primary_light</item>
        <item name="android:textColorSecondary">@color/etr_text_secondary_light</item>
        <!-- Status bar colors. -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <!-- Font. -->
        <item name="android:fontFamily">@font/dotgothic16</item>
    </style>

    <style name="CustomAlertDialogStyle" parent="Theme.MaterialComponents.Light.Dialog.Alert">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:background">@drawable/bg_alert_dialog</item>
        <item name="android:windowNoTitle">true</item>
        <item name="colorPrimary">@color/etr_black</item>
        <item name="colorOnSurface">@color/etr_black</item>
    </style>
</resources>